pipeline_id = "motion_detector_v1"
base = "motion_only_v1"

[verification]
kind = "object_detector"
model_path = "models/optional_actor_detector.pt"
confidence_threshold = 0.25
active_seconds_after_motion = 2.0
run_every_n_frames = 3

[attribution]
ambiguity_margin = 0.10
record_ambiguous_targets = true
